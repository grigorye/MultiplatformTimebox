import XCTest
@testable import MultiplatformTimeboxViews

final class MultiplatformTimeboxViewsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MultiplatformTimeboxViews().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
